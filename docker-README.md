<style>
  body {
    direction: rtl;
    text-align: right;
  }
</style>

### **گزارش کار فاز امتیازی +۲: داکرایز کردن پایگاه داده و بک‌اند**

  * **نام پروژه:** پلتفرم جامع خرید و فروش بلیط سفر
  * **درس:** پایگاه داده‌ها
  * **استاد:** خانم دکتر پیشگو
  * **دستیاران آموزشی:** مهندس علیرضا قربانی، مهندس محمد حسین هوشمند
  * **نام اعضای گروه:** سید احمدرضا موسوی، امیرحسین باقری، شهرزاد عروجی
  * **لینک GitHub پروژه:** (لینک ریپازیتوری را اینجا قرار دهید)

-----

#### **ساختار پروژه و فایل‌های Docker**

  * **نمای کلی ساختار:**
    ```bash
    /db-project
    |-- /config      # setting folder
    |-- Dockerfile
    |-- docker-compose.yml
    |-- requirements.txt
    |-- manage.py
    |-- .env         
    `-- README.md
    ```
  * **توضیح فایل‌ها:**
      * `Dockerfile`: این فایل شامل دستورالعمل‌های ساخت ایمیج (Image) داکر برای سرویس بک‌اند (اپلیکیشن جنگو) است.
      * `docker-compose.yml`: این فایل ارکستریتور اصلی پروژه است که تمام سرویس‌ها (وب، دیتابیس، Redis، Celery و...)، شبکه‌ها و والیوم‌ها را تعریف و مدیریت می‌کند.
      * `requirements.txt`: لیست تمام وابستگی‌ها و پکیج‌های پایتون مورد نیاز پروژه.
      * `.env`: این فایل برای نگهداری متغیرهای محیطی مانند اطلاعات اتصال به پایگاه داده و کلیدهای حساس استفاده می‌شود تا از قرار دادن آن‌ها در کد جلوگیری شود.

-----

#### **پیاده‌سازی و توضیح فایل‌ها**


**داکرایز کردن بک‌اند (فایل `Dockerfile`)**

این فایل مسئولیت ساخت ایمیج سرویس وب ما را بر عهده دارد که با زبان پایتون و فریمورک جنگو پیاده‌سازی شده است.

  * **محتوای `Dockerfile`:**

    ```dockerfile
    FROM python:3.11-slim

    LABEL org.opencontainers.image.authors="safar.com"

    ENV PYTHONDONTWRITEBYTECODE=1
    ENV PYTHONUNBUFFERED=1

    WORKDIR /code  

    COPY requirements.txt .

    RUN apt-get update && apt-get install -y libpq-dev gcc
    RUN pip install --no-cache-dir -r requirements.txt  

    COPY . .
    ```

  * **توضیح دستورات:**

      * `FROM python:3.11-slim`: ایمیج پایه ما یک نسخه بهینه و کم‌حجم از پایتون 3.11 است.
      * `ENV ...`: این متغیرهای محیطی برای بهینه‌سازی اجرای پایتون در محیط داکر تنظیم شده‌اند.
      * `WORKDIR /code`: دایرکتوری کاری پیش‌فرض در داخل کانتینر را `/code` تعیین می‌کند.
      * `COPY requirements.txt .`: برای استفاده بهینه از کش داکر، ابتدا فقط فایل نیازمندی‌ها کپی می‌شود.
      * `RUN ...`: در این بخش، ابتدا ابزارهای مورد نیاز برای کامپایل برخی پکیج‌های پایتون (مانند `psycopg2`) نصب شده و سپس تمام نیازمندی‌های پروژه از طریق `pip` نصب می‌شوند.
      * `COPY ..`: در نهایت، تمام فایل‌های پروژه به داخل کانتینر کپی می‌شوند.

**مدیریت سرویس‌ها (فایل `docker-compose.yml`)**

  * **توضیح سرویس‌ها:**

      * **`web` (سرویس بک‌اند):**

          * `build: .`: ایمیج این سرویس را با استفاده از `Dockerfile` موجود در همین مسیر می‌سازد.
          * `command`: دستور اجرای سرور توسعه جنگو را مشخص می‌کند.
          * `volumes`: کد پروژه را به داخل کانتینر `mount` می‌کند تا تغییرات در کد به صورت زنده اعمال شوند.
          * `ports: - "8000:8000"`: پورت ۸۰۰۰ سیستم میزبان را به پورت ۸۰۰۰ کانتینر متصل می‌کند.
          * `depends_on`: تضمین می‌کند که این سرویس پس از اجرای کامل سرویس‌های `db`, `redis` و `elasticsearch` اجرا شود.
          * `environment`: متغیرهای محیطی مورد نیاز برای اتصال به سایر سرویس‌ها را به کانتینر تزریق می‌کند.

      * **`db` (سرویس پایگاه داده):**

          * `image: postgres:17-alpine`: از ایمیج رسمی و بهینه PostgreSQL استفاده می‌کند.
          * `volumes`: از یک `named volume` به نام `postgres_data` برای ذخیره‌سازی دائمی داده‌های پایگاه داده استفاده می‌کند تا با خاموش شدن کانتینر، داده‌ها از بین نروند.
          * `ports: - "5433:5432"`: پورت ۵۴۳۲ کانتینر (پورت پیش‌فرض Postgres) را به پورت ۵۴۳۳ سیستم میزبان متصل می‌کند.

      * **`redis` (سرویس کشینگ):**

          * `image: redis:7-alpine`: از ایمیج رسمی Redis برای مدیریت کش و وظایف پس‌زمینه (Celery) استفاده می‌کند.
          * `volumes`: داده‌های Redis نیز برای پایداری در یک `named volume` ذخیره می‌شوند.

      * **`celery` (سرویس پردازش پس‌زمینه):**

          * این سرویس که از همان ایمیج بک‌اند ساخته می‌شود، وظیفه اجرای پردازش‌های سنگین و زمان‌بر (مانند ارسال ایمیل یا پیامک) را در پس‌زمینه بر عهده دارد تا اپلیکیشن اصلی سریع و پاسخگو باقی بماند.

      * **`elasticsearch` و `kibana` (سرویس جستجوی پیشرفته):**

          * این دو سرویس برای پیاده‌سازی قابلیت جستجوی سریع و قدرتمند (که در فاز چهارم پروژه خواسته شده) به کار می‌روند. Elasticsearch موتور جستجو و Kibana ابزار مدیریت و مشاهده داده‌های آن است.

-----

#### **راهنمای اجرا و تست پروژه**

**پیش‌نیازها**

  * نصب بودن Docker و Docker Compose بر روی سیستم.

**مراحل راه‌اندازی**

1.  ابتدا پروژه را از گیت‌هاب کلون کنید.
2.  در ریشه اصلی پروژه، یک فایل با نام `.env` بسازید. محتوای این فایل باید شامل متغیرهای محیطی باشد که در `docker-compose.yml` تعریف شده‌اند.
3.  دستور زیر را در ترمینال اجرا کنید تا تمام سرویس‌ها ساخته و در پس‌زمینه اجرا شوند:
    ```bash
    docker-compose up -d --build
    ```

**بررسی وضعیت سرویس‌ها**

  * بر
ای مشاهده لاگ‌های سرویس بک‌اند و اطمینان از اجرای صحیح آن، از دستور زیر استفاده کنید:
    ```bash
    docker-compose logs -f web
    ```
**تست APIها**

  * پس از اجرای موفقیت‌آمیز سرویس‌ها، اپلیکیشن شما بر روی آدرس `http://localhost:8000` در دسترس خواهد بود. برای تست APIها می‌توانید از ابزارهایی مانند Postman یا دستور `curl` استفاده کنید. برای مثال:
    ```bash
    
    curl -X GET http://localhost:8000/api-test/cities-list/
    ```

**توقف سرویس‌ها**

  * برای متوقف کردن و حذف تمام کانتینرها، شبکه‌ها و والیوم‌های ساخته شده، از دستور زیر استفاده کنید:
    ```bash
    docker-compose down
    ```

